{
  "routes": [
    {
      "method": "GET",
      "path": "/list/:id",
      "handler": "versioning.list",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/:id",
      "handler": "versioning.getVersion",
      "config": {
        "policies": []
      }
    }
  ]
}
