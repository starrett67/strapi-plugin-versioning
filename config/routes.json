{
  "routes": [
    {
      "method": "GET",
      "path": "/list/:id",
      "handler": "versioning.list",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/:id",
      "handler": "versioning.getVersion",
      "config": {
        "policies": []
      }
    },
    {
      "method": "PUT",
      "path": "/restore/:id",
      "handler": "versioning.restore",
      "config": {
        "policies": []
      }
    }
  ]
}
